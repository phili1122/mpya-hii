# Removed client-side secrets. Server-side environment variables should be set in the hosting platform (Vercel). Do NOT use VITE_-prefixed vars for secrets.
# Example server env vars (do NOT commit real secrets):
# TURSO_CONNECTION_URL=libsql://<your-db>.turso.io
# TURSO_AUTH_TOKEN=<your-secret-token>

# For local development, set above vars in a system environment or use a local .env.local that is gitignored.
