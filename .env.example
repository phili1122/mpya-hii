# Example environment variables — DO NOT commit real secrets here.
# Set these variables in your server/hosting environment (Vercel Dashboard > Project Settings > Environment Variables).

# Database URL (Turso/libSQL)
TURSO_CONNECTION_URL=libsql://your-db-name.region.turso.io

# Database auth token (server-side secret)
TURSO_AUTH_TOKEN=your-secret-token
